List all bayer password policies
---
tags:
  - AuthServices
parameters:
  - name: culture
    in: query
    type: string
    description: Optional parameter cultureName(Default is en-US)
security:
    - basicAuth: []
responses:
  200:
    description: Returns a list of all password policies,
    schema:
      $ref: '#/definitions/return-password-policy'
